# --- PDF ingestion ---
pypdf==4.3.1

# --- Embeddings + cross-encoder reranking ---
sentence-transformers==3.0.1

# --- Sparse (BM25) + dense (FAISS) retrieval ---
rank-bm25==0.2.2
faiss-cpu==1.8.0

# --- LLM inference ---
torch==2.3.1
# Pinned to match torch==2.3.1 exactly (PyTorch's official compat table).
# Not used directly by this project, but sentence-transformers' CLIP
# model-card code imports it unconditionally; Colab preinstalls a much
# newer torchvision, and installing torch==2.3.1 without also pinning
# torchvision leaves that newer/older pair mismatched, breaking on
# `torch.library.register_fake` (added in torch 2.4) at import time.
torchvision==0.18.1
transformers==4.44.2
accelerate==0.33.0
bitsandbytes==0.43.3
# Pinned to match torch==2.3.1's official paired Triton release. Not
# imported directly by this project, but bitsandbytes==0.43.3
# unconditionally imports its Triton-accelerated matmul module at
# import time, which expects `triton.ops` — removed/restructured in
# newer Triton releases. Colab preinstalls a much newer Triton than
# bitsandbytes==0.43.3 was built against; without this pin the newer
# one stays in place and breaks on `ModuleNotFoundError: No module
# named 'triton.ops'` when loading any 4-bit quantized model. Same
# mismatch shape as the torchvision pin above, different package.
triton==2.3.0

# --- Orchestration ---
langchain==0.2.14
langchain-community==0.2.12

# --- Evaluation ---
ragas==0.1.16
rouge-score==0.1.2
bert-score==0.3.13
datasets==2.20.0

# --- Deployment ---
streamlit==1.37.1

# --- Testing ---
pytest==8.2.2

# --- Utilities ---
python-dotenv==1.0.1
numpy==1.26.4
